spring:
  datasource:
    url: jdbc:mysql://localhost:3306/marwenPFE
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: MYSQL  # Use "MYSQL" instead of "mysql"
    database-platform: org.hibernate.dialect.MySQLDialect  # Correct the dialect class name
